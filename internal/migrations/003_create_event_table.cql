CREATE TABLE IF NOT EXISTS event (
                                     id INT PRIMARY KEY,
                                     image TEXT,
                                     name TEXT,
                                     description TEXT,
                                     date TIMESTAMP,
                                     registered_users TEXT
);